<h1>Listing users to assign to <%= @position.title %></h1>

<table>
  <tr>
    <th>First name</th>
    <th>Last name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.first_name %></td>
    <td><%= user.last_name %></td>
	<td><input type="checkbox" name="assign" value="unchecked" /></td>
  </tr>
<% end %>

</table>
<br />
