<h1>Listing users to assign to <%= @position.title %></h1>

<table>
  <tr>
    <th>First name</th>
    <th>Last name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.first_name %></td>
    <td><%= user.last_name %></td>
	<td><%= check_box_tag(:assigned) %><%= label_tag(:assigned, "") %></td>
  </tr>
<% end %>

</table>
<br />
