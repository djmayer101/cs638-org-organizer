<%= form_for(@duty_assignment) do |f| %>
  <% if @duty_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@duty_assignment.errors.count, "error") %> prohibited this duty_assignment from being saved:</h2>

      <ul>
      <% @duty_assignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
